# frozen_string_literal: true

RSpec.describe Wiggle do
  it "has a version number" do
    expect(Wiggle::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
